import { describe, it } from 'mocha'

describe('contract from readme', () => {
  it('should write result file and exit with zero, when tests are successful')
  it('should write result file and exit with zero, when tests aren\'t succesful')
  it('should capture stdout from tests')
  it('should capture stderr from tests')
  it('should exit with non-zero code with an exception outside of tests')
})
